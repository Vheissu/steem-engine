type Sellbook {
    txId: String!
    timestamp: Int!
    account: String!
    symbol: String!
    quantity: String!
    price: String!
    expiration: Int!
}

type Query {
    sellBook(symbol: String!, account: String): [Sellbook!]!
}