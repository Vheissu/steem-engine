type Buybook {
    txId: String!
    timestamp: Int!
    account: String!
    symbol: String!
    quantity: String!
    price: String!
    tokensLocked: String!
    expiration: Int!
}

type Query {
    buyBook(symbol: String!, account: String): [Buybook!]!
}