type Balance {
    account: String!
    symbol: String!
    balance: String!
}

type Query {
    balances(account: String!): [Balance!]!
    tokenBalance(symbol: String!, account: String!): [Balance!]!
}